<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- 通知権限（Foreground Service の常駐通知用、Android 13+ で要求） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- アプリ切替でプロセスが殺されるのを防ぎ SSH セッションを生かす -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- SSH セッションの常時維持は Google のガイドでも specialUse の想定ユースケース。
         dataSync は targetSdk 35+ で 6 時間タイムアウト→強制クラッシュがあるため specialUse を使う。 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:name=".AnotermApp"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:windowSoftInputMode="stateHidden|adjustNothing"
        android:theme="@style/Theme.Anoterm">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|locale|layoutDirection"
            android:windowSoftInputMode="stateHidden|adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- SSH セッションが 1 つでも生きている間プロセスを延命する Foreground Service -->
        <service
            android:name=".ssh.SshForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps interactive SSH terminal sessions alive while the app is backgrounded" />
        </service>
    </application>

</manifest>
