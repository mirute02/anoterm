# 更新履歴

日付は [リリース](https://github.com/mirute02/wanoterm/releases) の公開日。
各版の APK はそこに置いてある。

## 0.16.1 — 2026-09-11

- **画像と文書がタップしても開かなかったのを直した。** パスを押すと状態はセットされて
  いたのに、それを受けてシートを描く側が存在しなかった。押しても何も起きないのは
  そのため
- 経緯: 0.8.0 でウィンドウ一覧を抽斗に作り替えたとき、目印から目印までをまとめて消す
  編集をして、その間にあった画像シートのブロックを巻き添えで消していた。0.10.0 の
  文書シートは、既に消えていたそのブロックを目印に挿入しようとして無言で失敗していた。
  どちらもコンパイルは通る（状態変数は使われているため）
- 他のシート（ヘルプ・履歴・鍵・メモ・tmux・切断確認・ブラウザ）も全部確認した。
  欠けていたのはこの 2 枚だけ

## 0.16.0 — 2026-09-11

- **端末だけの全画面を追加**（⋮ メニューの「端末だけにする」）。上のバーも tmux の行も、
  **システムのバーも**消える。「端末だけ」と言う以上、上の 24dp を残す理由が無い
- 残るのは端末と浮遊パッド。画面下 1/4 のタップでキーボードは今までどおり出るし、
  キーボードを出せば補助キー列も今までどおり付いてくる
- 抜けるのは**戻る操作**。バーが無い以上、押す場所ではなく元から指が知っている操作に
  割り当てないと、入ったきり出られない。システムのバーは端から掃けば一時的に戻る
- 状態は覚える。読むための姿勢であって、その都度入り直す物ではない。忘れて起動しても
  戻る操作でいつでも抜けられる
- 画面を離れるときはシステムのバーを必ず戻す（消したまま別の画面へ行くと、そちらで
  操作できなくなる）

## 0.15.0 — 2026-09-11

- **タブを上のバーの中に畳んだ。1 行減る。** タブの名前はそのまま「今どの接続にいるか」で、
  バーの見出しと役割が重なっていた。重なっているものを 2 行に分けて置くのは、
  狭い画面では高く付く。接続が 1 本のときは従来どおり見出しを出す
- × は選んでいるタブにだけ出す。閉じるのは今見ている物で、並んだ × は幅を食ううえ
  誤タップの的が増える。どのタブも長押しで閉じられるのは従来どおり
- 接続状態の丸は、選んでいるタブの中に入った
- **tmux のウィンドウ行を隠せるようにした**（⋮ メニュー）。隠しても行き先は消えない。
  抽斗 (≡) に全接続のウィンドウが並んでいる。1 タップが 2 タップになる代わりに 1 行返る。
  両方隠せば上は 1 行
- **浮遊パッドを動かすとワープすることがあったのを直した**。位置を px で持ち、
  `remember` の key に「使える幅」を入れていたため、ブラウザを開いた・tmux の行が出た
  などで幅が変わると、その瞬間に位置が作り直されて飛んでいた。さらに `pointerInput` の
  key にも入っていたので、幅が変わると掴んでいた指のジェスチャごと取り消され、
  動かした分が捨てられていた。位置を比率のまま持ち、掴んでいる間は幅が変わっても
  ジェスチャを切らないようにした

## 0.14.1 — 2026-09-11

- **0.14.0 で入れた権限モードの表示を取り消した。** モード自体は Claude Code の
  セッション記録から読めるが、どの記録がこのウィンドウのものかは tmux のペインの
  現在地からの当て推量で、同じディレクトリに複数立てていると取り違える。
  「承認が自動かどうか」を間違って示す表示は、表示が無いより悪い
- ⇧Tab のキーは残る（浮遊パッドの右上と補助キー列）。押せば権限モードは回る。
  回った先が何かは、Claude Code 自身の表示で確かめること

## 0.14.0 — 2026-09-11

- **浮遊パッドに ⇧Tab を追加し、今どちらのモードかを点灯で示すようにした。**
  承認を聞かれるのはキーボードを閉じて読んでいる時なので、そこから 1 タップで届くのが筋
- 点灯の意味は 3 段階。消灯 = 聞かれる（またはモードが読めていない）、点灯 = 聞かずに進む、
  警告色 = 何も確認しない (bypassPermissions)。**読めていないときに「聞かれない」と
  光らせない**（嘘を吐くくらいなら消しておく）
- モードは画面の文字から判定していない。Claude Code がセッション記録に書いている
  `permissionMode` をそのまま読む。表示が変わっても壊れない。読み直すのはタブを移った時と
  ⇧Tab を送った直後だけ
- どの記録がこのウィンドウのものかは tmux のペインの現在地から辿る当て推量なので、
  同じディレクトリで複数立てていると外すことがある。外したら「分からない」に落ちる
- 抽斗の右上にモード名も出る。点灯の色を覚えなくても読める場所を 1 つ用意した
- 上の 2 隅（キーボードと ⇧Tab）は数字より小さく色も別にしてある。答えるつもりで
  モードを切り替えるのは、その逆より取り返しがつかない

## 0.13.0 — 2026-09-11

- **補助キー列に ⇧Tab を追加**。Shift+Tab (CSI Z) を送る。Claude Code はこれで権限モードを
  切り替えるので、自動承認に一度合わせれば以後は聞かれなくなる。ソフトキーボードでは
  Shift を押しながら Tab が実質打てず、承認のたびに 1 を押す作業から降りる手段が
  端末側に無かった

## 0.12.1 — 2026-09-11

- **アプリを立ち上げるとキーボードが出てくるのを直した（再発分）**。0.10.2 で塞いだのは
  アプリ側の 3 経路で、今回のはシステム側だった。マニフェストに `adjustNothing` としか
  書いておらず、開いた状態を指定していなかったため、システムが「テキスト入力欄を名乗る
  view に focus が乗った」と見て気を利かせていた。`stateHidden` を明示した
- あわせて `requestFocusFromTouch()` をやめた。あれは「指で触ったので focus が移った」と
  名乗るもので、IME を自動で出す条件そのもの。`isFocusableInTouchMode` が立っているので
  素の `requestFocus()` で足りる

## 0.12.0 — 2026-09-11

- **ウィンドウ一覧が「tmux なし」としか言わなかった原因を突き止めて直した。**
  項目の区切りに 0x1F の 1 バイトを使っていたが、**tmux はそれを `\037` という 4 文字の
  綴りに置き換えて返していた**。こちらは 0x1F で切っていたので 1 行も読めず、
  tmux は正しく答えているのに一覧は空、理由も無いので「tmux なし」に落ちていた
- 両方の形で読めるようにした。生の 0x1F でも、見える形の `\037` でも切れる
- 同じ経路を使う tmux の切り替えバーも一緒に直る
- 実際に返ってくる形のテストを追加

## 0.11.1 — 2026-09-11

- ウィンドウが 1 件も読めなかったとき、**返ってきた物の見本を出す**ようにした。
  区切り文字が何個含まれていたか、区切りを含む行が何行あったか、そして先頭行そのもの
  （制御文字は記号に置き換え、60 字まで）。「7 行返って 0 行解釈」までは分かっても、
  区切りが届いていないのか、tmux 以外の何かが混ざっているのかは実物を見ないと決まらない
- あわせて **tmux の解析にテストを 5 本足した**。正しい形の入力なら読めること、
  名前に空白が入っても壊れないこと、ログインシェルの挨拶が混ざっても別のウィンドウとして
  読まないこと、区切りが落ちたら 1 件も取らないこと（読み違えて別のウィンドウを
  選ばせるくらいなら何も出さないほうがよい）

## 0.11.0 — 2026-09-11

- **左の余白を端末の中だけに掛けるようにした**。画面全体に padding を掛けていたので、
  上のバーもタブもブラウザも一緒に寄り、しかも余白の帯だけ端末の背景から外れて
  わざとらしい色の段差になっていた。描画そのものの開始位置をずらす形に変えたので、
  背景は端まで端末の色のまま、文字だけが右へ寄る。桁数・タップ位置・選択ハンドル・
  カーソル通知も余白ぶん合わせてある
- **抽斗に版を出すようにした**。手元の端末に何が入っているのか確かめる手段が無いと、
  「直したはずのものが直っていない」ときに、入っていないのか効いていないのかを
  切り分けられない
- **「tmux なし」が 3 つの場合を区別するようになった**。理由が返っていればその一行
  (入っていない / サーバー未起動 / PATH に無い)、応答があってウィンドウ 0 件なら
  「何行返って何行解釈できたか」、それ以外は従来の文言。ウィンドウが出ない原因を
  アプリ自身が言えるようにした

## 0.10.2 — 2026-09-11

- **キーボードが勝手に出てこなくなった**。「焦点を取る」と「キーボードを出す」が
  1 つの関数に入っていたため、焦点が移るあらゆる場面でせり上がってきていた。
  タブをスワイプした時、シートを閉じた時、ホームから戻った時、画面を読み直した時。
  分離して、出すのは利用者がそう言った時だけにした
- 焦点自体は今までどおり取る。取っていないと物理キーボードの入力が届かず、
  後から「出せ」と言われたときの宛先も無くなる。開いていたキーボードはそのまま残る
- キーボードを出す道は 3 つだけ: 画面下 1/4 のタップ、補助キー列の ⌨ ボタン、
  浮遊パッドの ⌨ ボタン

## 0.10.1 — 2026-09-11

- **内側カメラに文字を食われないようにした**。折りたたみを開いた画面の中にカメラがあり、
  そこに重なった文字が読めなくなっていた。端末を回すとカメラは上にも横にも来るので、
  位置を決め打ちにはできない。システムが持っている「画面のうち塞がっている所」
  (displayCutout) を見て、左右ぶんだけ自動で避ける
- あわせて **左端の余白を設定できるようにした**（0〜48dp）。カメラが画面の下に隠れている
  種類のものだと、システムはそこを「塞がっている」と報告しないことがある。
  自動で避けきれない場合はこちらで開ける

## 0.10.0 — 2026-09-11

- **テキストファイルもタップで開くようになった**。`.kt` `.md` `.json` `.diff` など、
  書き換えられがちな綴りに下線が付く。行番号付き・折り返し無しで出す（コードを
  折り返すと字下げが崩れて構造が読めない）。書かれたばかりの物を、端末を離れずに
  確かめるためのもの
- **履歴の検索を入れた**。⋮ メニューの「検索」。この接続で流れた文字を探す
  （シェルの履歴でもファイルの中身でもない）。一致は塗られ、矢印で行き来できる。
  探している物はたいてい「さっき流れた」ものなので、新しい側の一致から見せる
- **ウィンドウ一覧が「tmux なし」としか言わなかったのを直した**。理由（tmux が
  入っていない／サーバーが動いていない／PATH に無い）はリモートから返ってきていたのに、
  一覧を組み立てる所で捨てていた。返ってきた一行をそのまま出す

## 0.9.0 — 2026-09-11

- **触った所にカーソルが移るようになった**。相手がマウス報告を要求している間だけ、
  タップした桁・行へ左クリックを送る。Claude Code の入力欄はマウスで位置を指せるので、
  タップをそのまま渡すだけで同じ事ができる。vim や mouse on の tmux でも効く。
  画面下 1/4 なら、カーソルを置いたうえでキーボードも開く（そこを触ったのは打ちたいから）
- これは「単タップは下 1/4 以外なにもしない」という原則の例外だが、相手がマウス報告を
  要求している間だけなので、読んでいる最中に誤爆する場面は無い

## 0.8.1 — 2026-09-10

- **ピンチで変えた文字の大きさが元に戻っていたのを直した**。ピンチはこの画面の中だけの
  値を変えていて、設定には書いていなかった。設定は元の値のままなので、再コンポーズが
  起きた瞬間（ブラウザを開く、タブが増える、設定を触る…）に古い値で上書きされていた。
  指を離した時に設定へ書き戻すようにしたので、アプリを閉じても残る

## 0.8.0 — 2026-09-10

- **ウィンドウ一覧が左から出る抽斗になった**。下から出るシートは「≡ を押して出てくる物」
  として読まれない。端からのスワイプで開く動きは切ってある（端末は左右スワイプで接続を
  行き来するので、左端を抽斗に取られると一番左のタブから隣へ移れない）。開いている間は
  横に払って閉じられる
- **スクロールに惰性を付けた**。以前は指が動いた分しか動かず、数百行遡るには何十回も
  擦る必要があった。端末の履歴は「少し戻る」より「かなり戻る」ほうが多い
- **遡っている間、下に「何行前」のボタンが出る**。押すと最新に戻る。読み終わって今に
  戻るのは、擦って戻る作業ではない
- **キーボードの上を 1 段減らした**。返答キー（1/2/3/y/n）が独立した段を占めていたので、
  補助キーの列に取り込んだ。ショートカット段と合わせて 3 段積まれると、狭い画面では
  本文が数行しか残らない

## 0.7.1 — 2026-09-10

- 分割中にキーボードを出しても、端末の行数が変わらないようにした。以前は端末とページが
  比率どおり両方縮み、打ち始めるたびに本文が動いていた。打っている時に見たいのは自分が
  打っている所なので、狭くなったぶんはページ側に負わせる
- ただしページを潰しきりはしない。残りの高さが足りなければ上限（8 対 2）で頭打ちになり、
  そこまでは近づく
- 左右に並べているときは効かない。キーボードは窓の下端を横いっぱいに取るので、
  隣り合った 2 面はどちらも同じだけ低くなる。片方だけ背を高いままにする置き方が無い

## 0.7.0 — 2026-09-10

- ブラウザを端末のブラウザに渡せるようにした。転送の出口は端末内の 127.0.0.1 なので、
  この端末の他のアプリからも届く。全画面で見たいときや DevTools を使いたいときに。
  渡しても分割の面は閉じない（閉じると穴も閉じて、渡した先が繋がらなくなる）
- 分割の向きと取り分を設定からも決められるようにした。仕切りは指でも動かせるが、
  狙った配分にぴったり合わせるのは難しく、折りたたみを開いた時と閉じた時で好みも変わる
- README に画像・ブラウザ・浮遊パッド・補助キーの説明を追加（日英）
- この更新履歴を追加

## 0.6.1 — 2026-09-10

- 相対パスを開けるようにした。0.6.0 は受け付けるようになっただけで、解決する側が
  入っておらず「見つからない」と出るだけだった。tmux にペインの現在地を尋ね、
  そこ → ホームの順に探す（ホームを先に見ると、同名のファイルがあったときに
  別の画像が開く）

## 0.6.0 — 2026-09-10

- **ページを端末の隣で見られるようにした**。端末に出た `http://` の URL に下線が付き、
  押すとブラウザが開く。通信は SSH 接続の中を通すので、`localhost` にしか口を開けて
  いない開発サーバーも外に晒さずに見られる。待ち受けは端末内の 127.0.0.1 に限定
- 上下・左右どちらの分割にもでき、仕切りをドラッグして配分を変えられる。向きと配分は
  覚えている
- https は転送せず直接開く。転送するとブラウザから見た宛先が 127.0.0.1 になり、
  証明書の名前が合わずに必ず警告になるため
- 履歴を遡っている間、コピーされる文字がずれていたのを直した。選択範囲を画面の行番号の
  まま grid から読んでいたが、遡っている間、画面上部の行はスクロールバックから来る
- パスの拾い方を緩くした。引用符で囲まれた空白入りの名前を拾い、`file://` を落とし、
  タップは左右 1 桁ぶん余裕を持たせた（1 行の高さは指より小さい）

## 0.5.0 — 2026-09-10

- **端末に出た画像パスをタップすると開くようにした**。下線が付き、押すとその場に絵が出る。
  読み込みは作業中のシェルとは別の SSH セッションで行うので、コマンドラインに `base64` が
  混ざらない
- kitty や iTerm2 のインライン画像プロトコルは実装していない。相手がそれを喋らないので、
  実装しても絵は出てこない
- 端末は行がどこで折り返したかを記録していないため、「上の行が右端まで埋まっていたら続き」
  という推定でパスを繋いでいる
- 5 MB まで。転送を始める前に大きさを見る。大きい画像は間引いて読む

## 0.4.0 — 2026-09-10

- **キーボードの出し入れで本文が消えていたのを直した**。画面が縮むとき、上端から溢れる行を
  スクロールバックに送らずそのまま捨てていた。広がるときは空行で埋めていた
- **読んでいる行が上下に飛ぶのを直した**。画面の高さが変わればスクロール位置も動かさないと
  同じ数字が別の行を指す。履歴を読んでいる間は上端が固定される
- **キーボードのアニメーション中、毎フレーム画面全体を描き直していたのを直した**。IME の
  可視判定に補間値を使っていたため、開閉の全フレームで再コンポーズが走っていた
- タブバーの伸び縮みを snap にした。滑らかに動くこと自体がガタつきとして読まれる
- 行間の設定が反映されていなかったのを直した
- ハンバーガーを左上に移した。戻る矢印は消え、右上のアイコン 7 個は「その他」1 つになった
- 補助キーがキーボードと一緒に出入りするようになった。浮遊パッドにキーボードボタンを追加
- ヘルプに「画面の操作」を追加

## 0.3.0 — 2026-09-09

- ウィンドウ一覧（≡）。接続 → セッション → ウィンドウの 3 階層で、どの接続のどの
  ウィンドウにも 1 タップで飛べる
- 更新のあったウィンドウに印。中で動いているコマンドを表示
- 浮かぶ返答パッド。行間の設定。tmux ステータス行の非表示
- prefix キーを一切送らなくなった。アタッチ先の切り替えが動いていなかったのを直した

## 0.2.1 — 2026-09-09

- tmux を prefix ではなくコマンドで操作するようにした
- tmux バーが空のときに理由を出すようにした

## 0.2.0 — 2026-09-09

- 最初の公開版
